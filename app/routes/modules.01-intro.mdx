---
title: "Getting Started"
---

export const meta = [{ title: `🐋 ${frontmatter.title}` }];

In this section, we'll get started with docker, running some simple containers and using the `docker` command line tool to inspect the state of our system.
