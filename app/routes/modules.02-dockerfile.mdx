---
title: "Dockerfile"
---

export const meta = [{ title: `🐋 ${frontmatter.title}` }];

In this section, we'll create a Dockerfile to build a simple web server written in golang. The web server will serve a simple "Hello, World!" message.
