---
title: "volumes"
---

export const meta = [{ title: `🐋 ${frontmatter.title}` }];

In this section, we'll explore how to use volumes to persist data in Docker containers.

We'll also look at how to use volumes to share data between the host machine and a container.
