---
title: "detached mode"
---

export const meta = [{ title: `🐋 ${frontmatter.title}` }];

In this section, we'll continue working on our `hello-world-go` project. When we last left off, we were able to access our application by publishing a port.

In this section, we'll explore some other modes of running and interacting with our container, specifically, running it is a background process.
