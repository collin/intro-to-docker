---
title: "Docker ports"
---

export const meta = [{ title: `🐋 ${frontmatter.title}` }];

In this section, we'll continue working on our `hello-world-go` project. When we last left off, we couldn't access our Go application from our browser. We'll fix that by exposing the Go application to the host machine.
